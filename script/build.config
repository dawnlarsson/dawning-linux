#!/bin/sh

echo "# Auto generated, do not edit." > artifacts/.config

cat config/info >> artifacts/.config
cat profile/any >> artifacts/.config

# Temporary inbetween profile for now
cat profile/limbo >> artifacts/.config

cat profile/$1 >> artifacts/.config

cat profile/debug >> artifacts/.config